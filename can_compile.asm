    section .text
main:
    push rbp
    mov rbp, rsp
    leave
    ret
sd:
    push rbp
    mov rbp, rsp
    leave
    ret
max:
    push rbp
    mov rbp, rsp
    leave
    ret
void_fn:
    push rbp
    mov rbp, rsp
    leave
    ret
