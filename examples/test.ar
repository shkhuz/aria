// fn good(a: u8, b: usize,) void {
//     {
//         return;
//         symb
//     }
//     symb;
//     sym
// }
fn h2__3wew__1234567890__(a: usize, b: u8) {
    {
        let i: u8 = a;
        let j: usize = b;
        let unsigned: i8;
    }

        let i: *const i8;
        let c: *i8 = i;

    // let x = 1;
    // (1) Type* -> pointer to Type
    // (2) Type** -> pointer to Type* 
    // let y = x + 2;
    // let z: u8;
    // let w = x + z; // x is now u8
    // but y

    // let i = a + b;
    //         ni  ni
}
