// RESOLVER TEST
// proc main() void {
//     let mut args: string = 12;
//     call(self);
//     proc code() *game {
//         let input;
//     }
//     let args = args;

//     proc code(identifier: string, identifier: u8) {
//         let identifier = 100;
//     }
//     proc call() {
//         hello;
//     }
// }

// ANALYZER TEST
// proc main() {
//     proc hello(a: usize, b: usize, c: usize) *u64 {
//         {
//         }
//     }

//     let val: u64 = hello(1, 2, 3);
// }

// CODE GENERATOR TEST
import coolmod;

// as of yet, compile-time variable values are not checked
// for overflow.
proc main(a: u8) {
    let b: usize = 12;
    coolmod::init(--b);
    let val = 256;
    let va = 256;
    let vl = 256;
    let al = 256;
    let vall = 256;
    let vd = -438737748478473847873847384784738437;
    let v: *u64;
    let i = a;
    i = 1;
    const dd: *u8 = pointer();
    b = -b;

    coolmod::init(--128);
    vl = 12;
}

proc code_me(a: u8, b: usize, c: i8) u8 {
    const aa: u8 = -128;
    b = -a;
    return b;
}

proc pointer(p: *u8) *u8 {
    const i = -p;
    const a: i8 = 129 - 1;
    const b = 2;
    const c: u8 = a + b;
    c
}
