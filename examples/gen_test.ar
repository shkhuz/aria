pub fn main(): u64 {
    a :: 12 + 2;
    a = 1;
    return {
        123;
        a
    };
}

fn add(a: u64, b: u64, c: u64, d: u64, e: u64, f: u64, g: u64, h: u64): u64 {
    a + b + c + d + e + f + g + h
}
