import "../std.ar";

fn fibbonacci(n: u64) u64 {
   return if (n == 0) 0
   else   if (n == 1) 1
   else (fibbonacci(n-1) + fibbonacci(n-2));
}

struct State {
    a: u8,
}

export fn main() void {
    std.writestring("Hello, world!");
    std.writechar(10);
    std.writeinteger(fibbonacci(30));
}
