#import "test.ar";

namespace std {
	struct foo {
		len: usize,
		len: a,
		ii: struct {
			i: struct {
				i: usize,	
			},
		},
	}

	fn main(argc: main) {
		let mut file: File;
		fn close_file(file: FILE) bool {

		}
	}
}
