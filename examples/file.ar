#import "test.ar";

namespace std {
	struct foo {
		len: usize,
		len: a,
		ii: struct {
			i: struct {
				i: usize,	
			},
		},
	}

	fn main(argc: main) {
		let mut file: File;
		{
			let file: usize = file;
			let file = file;
		};
		fn close_file(file: FILE) bool {

		}
	}
}
