#import "std";

fn main {
    game_engine := game_engine_new();
    game_engine_run(game_engine);
}

fn game_engine_new GameEngine {
    game_engine: GameEngine;
}

fn game_engine_run(game_engine: GameEngine) {
}

struct GameEngine {
    width: u64,
    height: u64,
    title: string,
}

