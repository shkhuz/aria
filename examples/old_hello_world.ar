
import "file.ar";

a :: 1.0;
b: u32 = 1;
c := new_game();
pointer: *u32 : &b;
value :: pointer.*;

arrlit :: .[u32]{a, b, 1, 2};
strlit :: .{.a = 1, .b = 2};

anumber :: @to(u8, 1);
while (a.next()) |a| {}
switch (a) {
    1, 2, 3 => {
        yield a;
    }
}

fn main() {
    mut b: u64 = 1;
}

const un = union(enum) {
    a: f32, 
    b: u32,
}
const val = un{ .a = 1 };

comp switch (val) {
    .a |val| => {},
    .b |val| => {},
}

main :: fn (u8) {
    stdout :: std.get_stdout();
    stderr :: std.get_stderr();

    stdout.write("hello");
    stderr.write('h');
    stderr.write("this is an example errorneous string");
    stderr.write('f');

    stderr.write(10);
    stderr.write(6.);
    stderr.write(453.55);
}

GameObject :: struct {
    a: u32, 
    b: f32,
};

std :: mod {
    writer :: trait {
        write :: fn (bytes: []u8) !void {
            syscalls.write("", .{@typeof(bytes)});
        }
    }

    stdout :: struct;
    impl writer for stdout {
        write :: fn (bytes: u8[]) result<u64, error> {
            syscall.write(STDOUT_FILENO, bytes, bytes.len);
        }
    }

    stderr :: struct;
    impl writer for stderr {
        write :: fn (bytes: u8[]) result<u64, error> {
            syscall.write(STDERR_FILENO, bytes, bytes.len);
        }
    }

    syscall :: mod {
        write :: extern fn (file: FILE*, bytes: u8*, len: u64);
    }

    // example "

    alloc :: fn<T> (obj: T) [T] {
        memory: [T]: malloc(@sizeof(obj));
        *memory = obj;
        memory
    }
}
