import "small2_import.ar" as c;

struct Ctx {
    a: u8,
    b: bool,
}

fn cool(a: u8) u8 {
    return a;
}

fn _main(a: i8) u8 {
    mut num: u8 = c.cool(1);
    mut ptr = &num;
    ptr.* = 2;

    mut f = &cool;
    f(1);

    return ptr.*;
}

// fn cool() void {}
// fn main() {
//     mut f = &cool;          // f is `*fn() void`
//     f.*();                  // callee type is `fn() void`
// }
