import "std";

struct S {
    a: u64,
}

fn get() S {
    mut s: S;
    s.a = 17;
    return s;
}

fn main() void {
    std.writeuinteger(get().a);

    imm x = get();
    imm ptr = &x.a;
    std.writeuinteger(ptr.*);
}
