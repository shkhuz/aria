fn main() void {
    fn cool(a: int, b: int) void {
        fn cool() df {cool;}
        mut name = cool(a,cool(),mutable({}, {fn cool() void {}}));
        imm war = if (cool) {call();} else cool;
        {mut name = {cool;};}
        
        if (main) {
            if (cool) 1 else {type Cool = Cool;}
        }
        func(1, 2);
        return 2;
        yield 1;
    }
}

type Main = ff;
type Main = *Main;

// (if (cond) 
//      ...
//  elseif (cond)
//      ... 
//  else
//      ...)
