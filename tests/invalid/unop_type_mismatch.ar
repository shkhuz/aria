fn main() {
    {
        const a: u8;
        const b = --a; // this is deliberate
    }

    {
        const a: u16;
        const b = -a;
    }

    {
        const a: u32;
        const b = -a;
    }

    {
        const a: u64;
        const b = -a;
    }
}

//! tests/invalid/unop_type_mismatch.ar:4:20: error: cannot negate unsigned operand of type `u8`
//! tests/invalid/unop_type_mismatch.ar:9:19: error: cannot negate unsigned operand of type `u16`
//! tests/invalid/unop_type_mismatch.ar:14:19: error: cannot negate unsigned operand of type `u32`
//! tests/invalid/unop_type_mismatch.ar:19:19: error: cannot negate unsigned operand of type `u64`
