fn main()
{
    {
        const a: u64;
        const b: u8;
        const c = a + b;
    }

    {
        const a: u64;
        const b: u16;
        const c = a + b;
    }

    {
        const a: u64;
        const b: u32;
        const c = a + b;
    }

    {
        const a: u64;
        const b: u64;
        const c = a + b;
    }

    {
        const a: i64;
        const b: i8;
        const c = a + b;
    }

    {
        const a: i64;
        const b: i16;
        const c = a + b;
    }

    {
        const a: i64;
        const b: i32;
        const c = a + b;
    }

    {
        const a: i64;
        const b: i64;
        const c = a + b;
    }

    {
        const a = 1;
        const b = -1;
        const c = a + b;
    }
}
